= form_for @matching do |f|
  - if @matching.errors.any?
    #error_explanation
      %h2= "#{pluralize(@matching.errors.count, "error")} prohibited this matching from being saved:"
      %ul
        - @matching.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
